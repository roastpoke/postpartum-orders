# GitHub + 腾讯云部署教程

> **最简单的部署方式：上传到GitHub，腾讯云自动部署**

---

## 🎯 部署流程概述

```
1. 上传代码到GitHub
   ↓
2. 腾讯云关联GitHub仓库
   ↓
3. 腾讯云自动部署
   ↓
4. 获得国内访问地址
```

**优点**：
- ✅ 不需要Git命令
- ✅ 不需要本地配置
- ✅ 腾讯云自动部署
- ✅ 国内访问速度快
- ✅ 完全免费

---

## 📋 准备工作

### 需要的账号
1. **GitHub账号**（已有）
2. **腾讯云账号**（已有）
3. **腾讯云CloudBase环境**（已创建：`cloudbase-2gvcaqwp505619c2`）

### 需要上传的文件
在 `e:\the\pwa-app` 目录下，需要这些文件：

```
e:\the\pwa-app\
├── index.html          ✅ 必需（主页面）
├── manifest.json       ✅ 必需（PWA配置）
├── sw.js              ✅ 必需（离线支持）
├── package.json       ✅ 必需（避免npm错误）
├── cloudbaserc.json    ✅ 必需（部署配置）
└── README.md          📖 说明文档
```

---

## 🚀 部署步骤

### 第一步：上传到GitHub（5分钟）

#### 方式1：通过GitHub网页上传（最简单）

1. **访问你的GitHub仓库**
   - 打开：https://github.com/roastpoke/postpartum-orders

2. **删除现有文件**（如果有错误的文件）
   - 点击进入 `pwa-app/` 文件夹（如果有）
   - 删除里面的所有文件
   - 返回仓库根目录

3. **上传新文件**
   - 点击 "Add file" → "Upload files"
   - 拖拽或选择这 **5个文件**：
     ```
     ✅ index.html
     ✅ manifest.json
     ✅ sw.js
     ✅ package.json
     ✅ cloudbaserc.json
     ```
   - 可选：也上传 `README.md`

4. **提交文件**
   - 在 "Commit changes" 框中填写：
     - Summary: `Fix deployment structure`
   - 点击 "Commit changes" 按钮

#### 方式2：替换GitHub仓库内容

如果仓库里有很多文件，可以：

1. **创建新仓库**（推荐）
   - 在GitHub创建新仓库，例如 `postpartum-orders-v2`
   - 按照上面的方式上传文件

2. **或清空现有仓库**
   - 在仓库设置中找到"Danger Zone"
   - 点击删除所有文件
   - 重新上传

---

### 第二步：配置腾讯云（3分钟）

1. **登录腾讯云CloudBase**
   - 访问：https://tcb.cloud.tencent.com/dev?envId=cloudbase-2gvcaqwp505619c2#/overview
   - 或访问：https://tcb.cloud.tencent.com
   - 选择你的环境：`cloudbase-2gvcaqwp505619c2`

2. **进入静态网站托管**
   - 点击左侧菜单"静态网站托管"
   - 点击"设置"或"配置"

3. **配置GitHub集成**
   - 找到"源码部署"或"Git平台部署"
   - 选择"GitHub"
   - 授权腾讯云访问你的GitHub

4. **选择GitHub仓库**
   - 选择仓库：`roastpoke/postpartum-orders`（或你的新仓库名）
   - 选择分支：`master` 或 `main`
   - 设置部署目录：`/`（根目录）

5. **保存配置**

---

### 第三步：触发部署（1分钟）

#### 方式1：自动部署
- 保存配置后，腾讯云会自动检测GitHub更新
- 自动部署开始

#### 方式2：手动触发
1. 在腾讯云控制台
2. 点击"部署"或"重新部署"按钮
3. 等待部署完成

---

### 第四步：访问网站

部署成功后，你会得到访问地址：

1. **默认域名**（腾讯云提供）
   ```
   https://cloudbase-2gvcaqwp505619c2.tcb.qcloud.la
   ```

2. **自定义域名**（可选）
   - 在"静态网站托管"→"设置"
   - 添加你的自定义域名
   - 配置DNS解析

---

## ✅ 验证部署成功

访问你的域名，应该能看到：
- 🎨 紫色渐变背景
- 📝 "月子中心订单管理系统"
- 🔘 4个快速登录按钮
- 📱 PWA安装提示

### 功能测试

1. **登录测试**
   - 点击任意快速登录按钮
   - 应该能登录并看到订单列表

2. **创建订单测试**
   - 切换到"创建订单"
   - 填写信息并提交
   - 应该能看到新订单

3. **权限测试**
   - 用"李主管"登录，看所有订单
   - 用"小张"登录，只看自己的订单

---

## 🔄 更新部署

当需要更新代码时：

1. **修改本地文件**
   - 编辑 `e:\the\pwa-app\` 目录下的文件

2. **上传到GitHub**
   - 在GitHub网页上替换文件
   - 或通过"Upload files"上传新版本

3. **腾讯云自动部署**
   - 腾讯云检测到GitHub更新
   - 自动重新部署
   - 几分钟后生效

---

## 🐛 常见问题

### 问题1：部署后还是404

**原因**：文件上传到了错误的目录

**解决**：
- 确保文件在GitHub仓库的**根目录**
- 不应该在 `pwa-app/` 或 `public/` 子目录

### 问题2：腾讯云找不到文件

**原因**：部署目录配置错误

**解决**：
- 在腾讯云配置中，部署目录设为 `/`
- 不要设为 `/public` 或其他

### 问题3：PWA不工作

**原因**：manifest.json 或 sw.js 路径错误

**解决**：
- 确保这两个文件在根目录
- 检查 index.html 中的路径引用

### 问题4：部署很慢

**原因**：首次部署需要安装依赖

**解决**：
- 耐心等待，首次可能需要5-10分钟
- 后续部署会快很多

---

## 💡 提示

### 最佳实践

1. **文件命名**
   - 使用小写字母
   - 不要用中文文件名
   - 不要有空格

2. **更新前测试**
   - 在本地浏览器打开 `index.html` 测试
   - 确认功能正常再上传

3. **版本管理**
   - 在GitHub创建Releases标记版本
   - 方便回滚

4. **备份**
   - 定期下载GitHub仓库作为备份
   - 或使用Git本地克隆

---

## 📱 生成二维码

部署成功后，生成二维码给员工：

1. **访问二维码生成器**
   - https://cli.im/
   - 或 https://www.liantu.com/

2. **输入你的网站地址**
   - 例如：`https://cloudbase-2gvcaqwp505619c2.tcb.qcloud.la`

3. **生成并下载二维码**
   - 可以打印出来
   - 或通过微信发送

4. **员工使用**
   - 用微信或浏览器扫描二维码
   - 打开网页
   - 安装到手机桌面

---

## 🎉 完成！

恭喜！你现在有一个：
- ✅ 国内可访问的网站
- ✅ 不需要VPN
- ✅ 自动部署
- ✅ 完全免费

---

## 📞 需要帮助？

如果遇到问题：
1. 检查文件是否在GitHub根目录
2. 查看腾讯云部署日志
3. 确认腾讯云环境状态

---

**部署成功后，就能分享给员工使用了！** 🎊
